#!/bin/bash

# create a function to return the hostname of the db server


host=`hostname -s`
cat <<EOF;
BEGIN;

CREATE OR REPLACE FUNCTION hostname()
RETURNS TEXT
LANGUAGE 'plpgsql'
AS \$EOR\$
  BEGIN
      RETURN '$host';
  END;
\$EOR\$
;

COMMIT;
EOF

resource <%= name %> {
    device    <%= device %>;
    meta-disk internal;
    disk      <%= disk %>;
  on <%= local %> {
    address   <%= local_ip %>:<%= port %>;
  }
  on <%= peer %> {
    address   <%= peer_ip %>:<%= port %>;
  }
}


# Permit the Director to contact this SD
Director {
    Name                    = <%= @director_name %>-dir
    Password                = "<%= @password %>"
<%= scope.function_template(['bacula/_tls.erb']) %>
}


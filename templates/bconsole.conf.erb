#
# Bacula User Agent (or Console) Configuration File
#

Director {
  Name = localhost-dir
  DIRport  = <%= scope.lookupvar('bacula::params::director_port') %>
  address  = localhost
  Password = "<%= password %>"
}


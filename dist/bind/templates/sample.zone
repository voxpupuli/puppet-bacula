$ORIGIN .
$TTL 604800	; 1 week
<%= bind_zone_name %>	IN SOA	<%= bind_zone_name%>. <%= bind_zone_contact %>. (
				0          ; serial
				604800     ; refresh (1 week)
				86400      ; retry (1 day)
				2419200    ; expire (4 weeks)
				604800     ; minimum (1 week)
				)
			NS	<%= bind_zone_ns %>

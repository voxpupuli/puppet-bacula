#!/bin/sh

trap "exit 1" INT

. ~interch/etc/server-list.sh

for i in $servers
do
    echo $i

	# Copy over the clear_cache script
    scp -p /root/bin/clear_cache2 $i:/root/bin/

	# Run the script
#    ssh $i 'time /root/bin/clear_cache'

done

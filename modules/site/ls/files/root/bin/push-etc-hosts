#!/bin/sh

. /var/lib/interchange/etc/server-list.sh

sshkey='/root/.ssh/id_dsa.bcs-push-robot-root'

# Push to web servers only
for d in $most_servers
do
    echo $d
    scp -i $sshkey -p /etc/hosts $d:/etc/
    #rsync -a -v --dry-run /etc/hosts $d:/etc/
    echo
done
